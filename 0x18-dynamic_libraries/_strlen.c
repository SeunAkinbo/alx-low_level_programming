#include "main.h"
/**
 * _strlen - Checke the length of a string
 * @s: The string to be checked
 * Return: The length of the string
 **/


int _strlen(char *s)
{
	int len = strlen(s);
	
	return (len);
}
