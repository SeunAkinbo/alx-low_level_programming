#include "main.h"

/**
 * mod - computs the moduls of the first integer by the second integer
 * @a: First Integer
 * @b: Second Integer
 * Return: the result
 **/

int mod(int a, int b)
{
	return (a % b);
}
