#include <stdio.h>
/**
 * main - Entry point
 *
 * Program prints a specific message using printf and returns 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* Prints the specific message*/
	printf("with proper grammar, but the outcome is a piece of art,\n");

	/* Returns 0*/
	return (0);
}
